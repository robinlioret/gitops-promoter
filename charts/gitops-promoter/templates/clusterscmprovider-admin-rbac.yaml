apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "gitops-promoter.fullname" . }}-clusterscmprovider-admin-role
  labels:
  {{- include "gitops-promoter.labels" . | nindent 4 }}
rules:
- apiGroups:
  - promoter.argoproj.io
  resources:
  - clusterscmproviders
  verbs:
  - '*'
- apiGroups:
  - promoter.argoproj.io
  resources:
  - clusterscmproviders/status
  verbs:
  - get
